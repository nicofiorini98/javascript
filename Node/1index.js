function sayHello(name)
{
    console.log('hello ' + name);
}


// sayHello('Nico');

//console.log(window); this i could do in browser, this in node is not defined, we don't
//have the window or doocument objects

//in node we have other objects to work with files, with the operating system, with the network and so on.