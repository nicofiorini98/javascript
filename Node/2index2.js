//GLOBAL OBJECT


console.log();  //console is what we call a global object, so it's part of the global scope

//wich meas we can access it anywhere in any files


setTimeout(); // we use this to call a function afet a delay, we can use this on the client, we can use this inside of a browser
                //or inside of node
clearTimeout();


setInterval() //we use to repeatedly call a function after a givend delay 
clearInterval() //which we use to stop that function from being called repeatedly


//these are the global object in javascript

//in browser we have this window object that represents our global scope

window.console.log();


